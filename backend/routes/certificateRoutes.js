// backend/routes/certificateRoutes.js
const express = require('express');
const router = express.Router();

// Placeholder route (replace with real certificate routes later)
router.get('/', (req, res) => {
  res.json({ message: "Certificate route works!" });
});

module.exports = router;
